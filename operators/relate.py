def relate(query_one, query_two, predicate=None):
    """Joins the two queries.
    If predicate is None, performs a natural join (default), otherwise joins
    according to the given predicate."""
    return query_one
